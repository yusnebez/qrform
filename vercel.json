{
  "buildCommand": "next build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "regions": ["cdg1"],
  "env": {
    "MONGODB_URI": "@mongodb_uri",
    "NEXT_PUBLIC_APP_URL": "${VERCEL_URL}"
  }
}
